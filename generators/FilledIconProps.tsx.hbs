import { Icon } from '../Icon'
import type { IconProps, {{ properCase name }}Props } from '../IconProps'

export const {{ properCase name }}: IconProps<{{ properCase name }}Props, true> = ({
  className,
  filled,
  ...rest
}) => <Icon className={className} {...rest}></Icon>
